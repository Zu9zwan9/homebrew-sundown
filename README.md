# Zu9zwan9/homebrew-sundown

Homebrew tap for [Sundown](https://github.com/Zu9zwan9/sundown).

```bash
brew install --HEAD Zu9zwan9/sundown/sundown
```

Head-only until there is a tagged release. `--HEAD` builds from `main`.
